const imageUtils = {
  // Add your image utilities here if needed
  getImageSource: (imageName) => {
    // Example utility function
    return require(`../assets/${imageName}`);
  },
};

export default imageUtils;
